# Slack Bot Tokens (Socket Mode)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-level-token
SLACK_SIGNING_SECRET=your-signing-secret

# Your Slack user ID (only this user can interact with the bot)
ALLOWED_USER_ID=U0XXXXXXXXX

# Default working directory for OpenCode (used if not set per-conversation)
OPENCODE_DEFAULT_DIR=/path/to/your/project

# OpenCode binary path (optional, defaults to "opencode")
OPENCODE_BIN=opencode
